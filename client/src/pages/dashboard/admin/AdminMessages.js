import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminMessages = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold">Messages & Notifications</Typography>
      <Typography>System-wide announcements and messaging control.</Typography>
    </Box>
  );
};

export default AdminMessages;
