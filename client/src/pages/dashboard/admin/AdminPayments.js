import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminPayments = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold">Payments & Billing</Typography>
      <Typography>View payments, invoices, and insurance records.</Typography>
    </Box>
  );
};

export default AdminPayments;
