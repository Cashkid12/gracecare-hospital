import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminMedicalRecords = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold">Medical Records Oversight</Typography>
      <Typography>View reports, manage lab results, and approve records.</Typography>
    </Box>
  );
};

export default AdminMedicalRecords;
