import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminContentManagement = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold">Content Management</Typography>
      <Typography>Update website content, services, and images.</Typography>
    </Box>
  );
};

export default AdminContentManagement;
