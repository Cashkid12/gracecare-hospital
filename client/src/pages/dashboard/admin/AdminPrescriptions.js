import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminPrescriptions = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold">Prescriptions Oversight</Typography>
      <Typography>View issued prescriptions and manage medicine lists.</Typography>
    </Box>
  );
};

export default AdminPrescriptions;
